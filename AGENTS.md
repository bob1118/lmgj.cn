# AGENTS.md

## 项目背景

- lmgj.cn：纺织品进出口贸易企业官网（主营针织 / 织造纺织品）。
- **`需求文档.md` 是唯一的决策来源**，任何开发前先通读它；其中的「待确认事项」（第 11 节）尚未定案，不要擅自替企业做决定（如询盘表单、托管平台选择）。
- `images/` 存放企业提供的素材（如 logo.png）。

## 关键决策（易错点）

- **单页站**：全部内容在一个页面，导航为锚点滚动，不要搭多页面路由。
- 页面区块固定为：Hero → 关于我们 → 产品中心 → 公司实力 → 联系我们。**没有新闻动态**，不要添加。
- **中英双语**：整页内容切换（中/EN 按钮），默认中文；文案必须中英成对维护。
- 产品仅按两大分类（针织 / 织造）展示代表产品，**无详情页、无搜索、无后台 CMS**。
- 部署在海外服务器（免备案），不要配置国内 ICP 备案相关内容。

## 技术栈

- **Astro 5**（静态输出）已初始化：`npm run dev` / `npm run check` / `npm run build` / `npm run preview`。
- 双语文案唯一源：`src/data/content.ts`，字段必须中英成对（`Pair`）；页面元素用 `.i18n-zh` / `.i18n-en` 成对 span 渲染，由 `html[data-lang]` 控制显隐。
- 织物纹样为共享 SVG pattern（定义在 `Layout.astro`，组件内以 `url(#patt-knit)` 等引用），勿重复定义。
- 双语切换、移动端菜单、滚动高亮的交互脚本都在 `Layout.astro` 尾部。
- 待企业素材到位后需替换：产品占位纹样图、微信二维码占位、联系信息占位（详见 README「待企业确认」表）。

## 内容约定

- 企业未提供的文案（中英简介、产品描述等）可起草初稿，但需在产出中注明「待企业确认」。
- 所有正文、注释、提交信息使用中文。
